Building a Django application to manage orders and user statistics. You will first implement a feature using Django's signal system, then uncover its common pitfalls, such as bypassing bulk operations and causing test isolation issues. Finally, you'll refactor the signal-based logic into a more explicit, maintainable, and performant service layer, a critical pattern for building scalable, production-ready applications.